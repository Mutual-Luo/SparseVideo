# Editable defaults for radial-attention.
# Model-specific blocks override only the listed keys; unspecified keys inherit
# from defaults. Dense warmup uses the same names and values for every backbone.
defaults:
  dense_warmup_step_ratio: 0.1 # Fraction of denoising steps that run dense before sparse attention starts.
  dense_warmup_layer_ratio: 0.03 # Fraction of earliest transformer layers that always run dense.
  decay_factor: 1 # Radial mask decay factor controlling sparsity by distance.
  block_size: 128 # Block size used by FlashInfer/Sage sparse mask construction.
  use_sage_attention: false # Route sparse attention through owned SageAttention instead of FlashInfer.

model_defaults:
  wan21-t2v-1.3b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.2
    block_size: 128
    use_sage_attention: false
  wan21-t2v-14b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.2
    block_size: 128
    use_sage_attention: false
  wan22-t2v-a14b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.3
    block_size: 64
    use_sage_attention: false
  hunyuan-t2v:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.95
    block_size: 128
    use_sage_attention: false
  wan21-i2v-14b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.2
    block_size: 128
    use_sage_attention: false
  wan22-i2v-a14b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.2
    block_size: 128
    use_sage_attention: false
  hunyuan-i2v:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.95
    block_size: 128
    use_sage_attention: false
  skyreels-v2-t2v-14b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.2
    block_size: 128
    use_sage_attention: false
  skyreels-v2-i2v-14b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.2
    block_size: 128
    use_sage_attention: false
  wan22-animate-14b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.2
    block_size: 128
    use_sage_attention: false
  wan21-vace-1.3b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.2
    block_size: 128
    use_sage_attention: false
  wan21-vace-14b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 0.2
    block_size: 128
    use_sage_attention: false
  cogvideox-t2v:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 1
    block_size: 128
    use_sage_attention: false
  cogvideox-i2v:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 1
    block_size: 128
    use_sage_attention: false
  ltx-video:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 1
    block_size: 128
    use_sage_attention: false
  ltx-video-i2v:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 1
    block_size: 128
    use_sage_attention: false
  allegro:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 1
    block_size: 128
    use_sage_attention: false
  mochi-1:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 1
    block_size: 128
    use_sage_attention: false
  easyanimate-v5-t2v-12b:
    dense_warmup_step_ratio: 0.1
    dense_warmup_layer_ratio: 0.03
    decay_factor: 1
    block_size: 128
    use_sage_attention: false
aliases: {}
